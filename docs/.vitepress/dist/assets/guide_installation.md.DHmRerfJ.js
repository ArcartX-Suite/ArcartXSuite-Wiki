import { _ as _export_sfc, o as openBlock, c as createElementBlock, ag as createStaticVNode } from "./chunks/framework.aOZh4hG0.js";
const __pageData = JSON.parse('{"title":"安装","description":"","frontmatter":{},"headers":[],"relativePath":"guide/installation.md","filePath":"guide/installation.md","lastUpdated":1779959189000}');
const _sfc_main = { name: "guide/installation.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="安装" tabindex="-1">安装 <a class="header-anchor" href="#安装" aria-label="Permalink to &quot;安装&quot;">​</a></h1><h2 id="环境要求" tabindex="-1">环境要求 <a class="header-anchor" href="#环境要求" aria-label="Permalink to &quot;环境要求&quot;">​</a></h2><table tabindex="0"><thead><tr><th>项</th><th>要求</th></tr></thead><tbody><tr><td>服务端</td><td>Spigot / Paper / mohist，<strong>MC 1.20.1及以上</strong></td></tr><tr><td>Java</td><td><strong>17</strong> 或更高</td></tr><tr><td>ArcartX 服务端插件</td><td>必装，版本与客户端 MOD 对齐</td></tr><tr><td>客户端 MOD</td><td>玩家必须安装 ArcartX 客户端 MOD</td></tr><tr><td>数据库</td><td>默认 SQLite；如需 MySQL，准备好访问凭证</td></tr></tbody></table><h2 id="依赖说明" tabindex="-1">依赖说明 <a class="header-anchor" href="#依赖说明" aria-label="Permalink to &quot;依赖说明&quot;">​</a></h2><p>AXS 的硬依赖只有 ArcartX。其他插件都按模块或功能降级处理：缺少非必要依赖时，主插件不应整体不可用。</p><table tabindex="0"><thead><tr><th>依赖</th><th>类型</th><th>影响</th></tr></thead><tbody><tr><td>ArcartX</td><td>必需</td><td>缺少时 AXS 无法启动</td></tr><tr><td>PlaceholderAPI</td><td>可选</td><td>只影响 AXS 的 PAPI 占位符注册和部分文本解析</td></tr><tr><td>MythicMobs / MythicBukkit</td><td>可选</td><td>缺少时 EntityTracker 的 Boss 追踪跳过，普通攻击目标 HUD 可继续使用</td></tr><tr><td>Chemdah</td><td>指定模块必需</td><td>缺少时 Conversation、QuestGPS 跳过加载</td></tr><tr><td>Adyeshach</td><td>可选</td><td>只影响对话 NPC 相关能力</td></tr><tr><td>MythicLib / AttributePlus / CraneAttribute / Vault / PlayerPoints / MMOItems / NeigeItems / AuthMe</td><td>可选</td><td>只影响对应模块的增强功能或桥接能力</td></tr><tr><td>OneBot 11 实现端（<a href="https://github.com/SnowLuma/SnowLuma" target="_blank" rel="noreferrer">SnowLuma</a> 推荐）</td><td>QQBot 模块必需</td><td>缺少时 QQBot 模块无法连接 QQ 群</td></tr><tr><td>MySQL</td><td>部分模块可选</td><td>Market 必需；QQBot / Mail / Chat 等跨服或持久化功能可选</td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">缺依赖时的预期行为</p><p>如果日志出现“某模块需要某插件，已跳过加载”或“跳过占位符注册”，这是正常降级。只有 ArcartX 缺失、模块配置错误、jar 损坏或付费模块授权失败，才需要按错误处理。</p></div><h2 id="步骤" tabindex="-1">步骤 <a class="header-anchor" href="#步骤" aria-label="Permalink to &quot;步骤&quot;">​</a></h2><h3 id="_1-安装-arcartx" tabindex="-1">1. 安装 ArcartX <a class="header-anchor" href="#_1-安装-arcartx" aria-label="Permalink to &quot;1. 安装 ArcartX&quot;">​</a></h3><p>按 <a href="https://wiki.arcartx.com/docs" target="_blank" rel="noreferrer">ArcartX 官方文档</a> 安装服务端插件，并向玩家分发客户端 MOD。</p><h3 id="_2-放入-axs-jar" tabindex="-1">2. 放入 AXS jar <a class="header-anchor" href="#_2-放入-axs-jar" aria-label="Permalink to &quot;2. 放入 AXS jar&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugins/</span></span>\n<span class="line"><span>├── ArcartX-x.x.x.jar</span></span>\n<span class="line"><span>└── ArcartXSuite-1.1.0-beta.jar</span></span></code></pre></div><h3 id="_3-启动一次-生成默认资源" tabindex="-1">3. 启动一次，生成默认资源 <a class="header-anchor" href="#_3-启动一次-生成默认资源" aria-label="Permalink to &quot;3. 启动一次，生成默认资源&quot;">​</a></h3><p>启动 / 重启服务端。AXS 会：</p><ul><li>把 jar 内 <code>.axb</code> 解密、解压成默认 YAML 释放到 <code>plugins/ArcartXSuite/</code></li><li>生成 <code>config.yml</code>（总开关 + 各模块启用状态）、<code>license.yml</code>（QQ + 授权码列表）和各模块配置</li><li>生成 <code>arcartx/ui/*.yml</code> UI 模板</li><li>全部模块<strong>默认 <code>enabled: false</code></strong></li></ul><h3 id="_4-验证" tabindex="-1">4. 验证 <a class="header-anchor" href="#_4-验证" aria-label="Permalink to &quot;4. 验证&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>/axs status</span></span></code></pre></div><p>所有行都是 <code>disabled / locked</code> 是正常的，因为还没填写授权码，也还没开启模块。</p><div class="info custom-block"><p class="custom-block-title">首次启动后不需要继续做什么</p><p>建议保持服务端运行，先编辑 <code>plugins/ArcartXSuite/license.yml</code> 填写 QQ 和授权码，再编辑 <code>plugins/ArcartXSuite/config.yml</code> 开启需要的模块。</p></div><h2 id="模块-jar-部署-可选" tabindex="-1">模块 Jar 部署（可选） <a class="header-anchor" href="#模块-jar-部署-可选" aria-label="Permalink to &quot;模块 Jar 部署（可选）&quot;">​</a></h2><p>AXS 1.1.0-beta 支持模块 Jar 独立部署。不放入模块 Jar 时，全部功能由宿主内置加载。</p><h3 id="结构" tabindex="-1">结构 <a class="header-anchor" href="#结构" aria-label="Permalink to &quot;结构&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugins/</span></span>\n<span class="line"><span>  ArcartXSuite.jar</span></span>\n<span class="line"><span>  ArcartXSuite/</span></span>\n<span class="line"><span>    config.yml</span></span>\n<span class="line"><span>    modules/                ← 按需放入模块 Jar</span></span>\n<span class="line"><span>      ArcartXSuite-RGB-1.1.0-beta.jar</span></span>\n<span class="line"><span>      ArcartXSuite-Tab-1.1.0-beta.jar</span></span>\n<span class="line"><span>      ArcartXSuite-Pickup-1.1.0-beta.jar</span></span>\n<span class="line"><span>      ...</span></span></code></pre></div><h3 id="工作原理" tabindex="-1">工作原理 <a class="header-anchor" href="#工作原理" aria-label="Permalink to &quot;工作原理&quot;">​</a></h3><ol><li>宿主启动时先扫描 <code>modules/</code> 目录，识别所有外部模块 Jar 的 id</li><li>对于有外部 Jar 的模块，宿主跳过内置加载，由模块 Jar 接管</li><li>对于没有外部 Jar 的模块，走内置加载（与之前版本行为一致）</li><li><code>config.yml</code> 中的 <code>enabled</code> 配置与 license.yml 授权对两种模式均生效</li></ol><h3 id="重载" tabindex="-1">重载 <a class="header-anchor" href="#重载" aria-label="Permalink to &quot;重载&quot;">​</a></h3><ul><li><code>/axs reload all</code> 自动判断每个模块的加载来源，走对应的重载路径</li><li><code>/axs reload &lt;模块名&gt;</code> 同理</li></ul><div class="tip custom-block"><p class="custom-block-title">无需手动 ax reload</p><p>ArcartX 现已支持 UI 自动导入，AXS 不再需要在启动或重载时执行 <code>ax reload</code> 命令。</p></div><h2 id="升级-替换-jar" tabindex="-1">升级 / 替换 jar <a class="header-anchor" href="#升级-替换-jar" aria-label="Permalink to &quot;升级 / 替换 jar&quot;">​</a></h2><ul><li>直接覆盖 jar 然后重启；<strong>不会丢已有数据库 / 已编辑过的 YAML</strong>。</li><li>新版本可能新增配置键；升级后建议备份后重启，让 <code>YamlConfigSynchronizer</code> 合并缺失字段。</li></ul><h2 id="卸载" tabindex="-1">卸载 <a class="header-anchor" href="#卸载" aria-label="Permalink to &quot;卸载&quot;">​</a></h2><ul><li>停服 → 删除 jar。</li><li>数据保留在 <code>plugins/ArcartXSuite/</code> 不会自动清理。</li></ul>', 32)
  ])]);
}
const installation = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  installation as default
};
