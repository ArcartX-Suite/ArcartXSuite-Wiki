import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"客户端包守卫 + 授权门控","description":"","frontmatter":{},"headers":[],"relativePath":"architecture/security.md","filePath":"architecture/security.md","lastUpdated":1778948966000}');
const _sfc_main = { name: "architecture/security.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="客户端包守卫-授权门控" tabindex="-1">客户端包守卫 + 授权门控 <a class="header-anchor" href="#客户端包守卫-授权门控" aria-label="Permalink to &quot;客户端包守卫 + 授权门控&quot;">​</a></h1><p>AXS 的安全由两部分组成：</p><ul><li><strong><code>ClientPacketGuard</code></strong>：防止伪造 / 高频回包 DoS。</li><li><strong><code>LicenseService</code></strong>：对付费模块执行 Worker 票据验签、离线缓存、机器指纹和资源密钥门控。</li></ul><h2 id="clientpacketguard" tabindex="-1">ClientPacketGuard <a class="header-anchor" href="#clientpacketguard" aria-label="Permalink to &quot;ClientPacketGuard&quot;">​</a></h2><p>每条客户端→服务端的包被抽象成 <code>(player, module, action)</code> 三元组。</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">client-packet-guard</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  enabled</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  cleanup-interval-ticks</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">200</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  defaults</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    window-ms</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1000</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    max-hits</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">20</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    mode</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;silent&quot;</span></span></code></pre></div><table tabindex="0"><thead><tr><th>字段</th><th>说明</th></tr></thead><tbody><tr><td><code>window-ms</code></td><td>时间窗（毫秒）</td></tr><tr><td><code>max-hits</code></td><td>时间窗内最多允许的回包数</td></tr><tr><td><code>mode</code></td><td><code>silent</code> 静默丢弃 / <code>notify</code> 丢弃并提示 / <code>punish</code> 执行命令</td></tr></tbody></table><h2 id="licenseservice" tabindex="-1">LicenseService <a class="header-anchor" href="#licenseservice" aria-label="Permalink to &quot;LicenseService&quot;">​</a></h2><p>付费模块加载前，<code>ModuleRegistry</code> 会检查当前授权决策：</p><ul><li><code>VALID</code> / <code>GRACE</code> / <code>EMERGENCY_GRACE</code> 且 ticket 包含该模块：允许加载。</li><li><code>AUTH_DENIED</code> / <code>DISABLED</code> / <code>NETWORK_ERROR</code> / <code>NOT_CONFIGURED</code>：跳过付费模块。</li></ul><p>授权身份由 <code>license.yml</code> 中的 <code>license.qq</code> 与 <code>license.keys</code> 组成。插件端不会再读取旧的 <code>license.key</code> 字段。</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">license</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  qq</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;1451759359&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  keys</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;AXS-SUITE-D58D6FAECBE18E94F15A&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  install_id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;auto&quot;</span></span></code></pre></div><p>Worker 票据会绑定：</p><table tabindex="0"><thead><tr><th>字段</th><th>作用</th></tr></thead><tbody><tr><td><code>ownerQq</code></td><td>授权码所属 QQ，必须与 <code>license.qq</code> 一致</td></tr><tr><td><code>subjectId</code></td><td><code>qq:&lt;QQ&gt;:install:&lt;install_id&gt;</code>，用于绑定当前服务器主体</td></tr><tr><td><code>installId</code></td><td>当前服务器安装 ID</td></tr><tr><td><code>fingerprintHash</code></td><td>当前服务器机器指纹</td></tr><tr><td><code>modules</code></td><td>多个授权码合并后的最终模块 entitlement</td></tr><tr><td><code>keyResults</code></td><td>每个授权码的成功/失败原因，用于 <code>/axs license status</code> 诊断</td></tr><tr><td><code>resourceKeys</code></td><td>已授权模块的资源密钥包装结果</td></tr></tbody></table><p>授权缓存位于：</p><div class="language-txt vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">txt</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>plugins/ArcartXSuite/security/license.cache</span></span>
<span class="line"><span>plugins/ArcartXSuite/security/secure-clock.dat</span></span>
<span class="line"><span>plugins/ArcartXSuite/security/local-salt.dat</span></span></code></pre></div><p><code>/axs license refresh</code> 只刷新当前绑定，不消耗换绑次数。<code>/axs license rebind</code> 是显式换绑操作，会请求 Worker 的 <code>/v1/rebind</code>，成功后旧绑定失效，新服务器成为 active binding。</p><p>付费模块资源走 <code>arcartx/internal/license/&lt;module&gt;/*.axl</code>，需要 ticket 中的 <code>resourceKeys</code> 解包后才能在内存中解密。资源密钥包装使用 <code>subjectId + installId + fingerprintHash + moduleId + localSaltHash</code> 派生本地解包密钥，因此复制 <code>license.cache</code> 到另一台服务器不会解锁资源。</p><p><code>local-salt.dat</code> 是绑定身份的一部分，不能把它看成普通临时缓存。删除它会让插件生成新的 local salt，继而改变 <code>localSaltHash</code> 和最终 <code>fingerprintHash</code>。即使 <code>license.yml</code> 里的 <code>install_id</code> 没变，Worker 也会把新的指纹视为另一个安装环境，并返回“授权码已绑定到其他服务器或旧机器指纹”。</p><p>网络不可达时，插件会进入本地 <code>license.cache</code> 校验。在线票据验签仍严格要求完整 <code>fingerprintHash</code> 匹配；离线缓存额外允许 VPN、虚拟网卡或网卡顺序变化导致的 <code>fingerprintHash</code> 变化，只要 <code>QQ + install_id + local-salt.dat + ticket 签名 + 有效期</code> 仍然匹配，就可以继续使用缓存中的付费模块和资源密钥。</p><p>排查这类问题时按顺序确认：</p><ol><li><code>license.yml</code> 的 <code>qq</code> 和 <code>keys</code> 是否属于同一个 QQ。</li><li><code>license.yml</code> 的 <code>install_id</code> 是否与后台 binding 一致。</li><li><code>/axs license fingerprint</code> 输出的 <code>localSaltHash</code> 是否与后台 binding 中的旧记录一致。</li><li>如果只有 <code>localSaltHash</code> 不一致，优先恢复旧 <code>security/local-salt.dat</code>；确实要迁移时再执行 <code>/axs license rebind</code>。</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("architecture/security.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const security = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  security as default
};
