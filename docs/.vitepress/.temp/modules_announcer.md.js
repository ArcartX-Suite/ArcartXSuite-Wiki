import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Announcer 播报系统","description":"","frontmatter":{},"headers":[],"relativePath":"modules/announcer.md","filePath":"modules/announcer.md","lastUpdated":1779851160000}');
const _sfc_main = { name: "modules/announcer.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="announcer-播报系统" tabindex="-1">Announcer 播报系统 <a class="header-anchor" href="#announcer-播报系统" aria-label="Permalink to &quot;Announcer 播报系统&quot;">​</a></h1><h2 id="功能定位" tabindex="-1">功能定位 <a class="header-anchor" href="#功能定位" aria-label="Permalink to &quot;功能定位&quot;">​</a></h2><p>服务器信息播报一站式方案，包含两大能力：</p><ul><li><strong>HUD 公告</strong> — 滚动轮播文字公告，文字从右向左滚动，可点击执行后台命令</li><li><strong>打字机字幕</strong> — 按帧播放文本动画，适用于剧情对白、任务提示、登录欢迎</li></ul><p><strong>几乎零依赖，适合作为第一个验证模块。</strong></p><h3 id="核心特性" tabindex="-1">核心特性 <a class="header-anchor" href="#核心特性" aria-label="Permalink to &quot;核心特性&quot;">​</a></h3><p><strong>公告：</strong></p><ul><li><strong>滚动轮播</strong>：多条公告按配置顺序自动轮播，文字从屏幕右侧向左滚动，滚完自动隐藏</li><li><strong>PAPI 解析</strong>：公告文本支持 <code>%player_name%</code>、<code>%server_online%</code> 等变量，按接收玩家实时解析</li><li><strong>点击命令</strong>：每条公告可绑定控制台命令，玩家点击 HUD 后自动执行（<code>&lt;player&gt;</code> 变量替换为点击玩家名）</li><li><strong>手动广播</strong>：通过命令加入队列或立即广播自定义文本，适合紧急通知</li><li><strong>跨服广播</strong>：启用 BungeeCord 代理转发后，手动广播（<code>broadcast</code> / <code>broadcastnow</code>）会自动同步到其他子服</li><li><strong>热重载</strong>：<code>/axs reload announcer</code> 即时生效，HUD 不中断</li></ul><p><strong>字幕：</strong></p><ul><li><strong>打字机动画</strong>：逐字播放文本，可控制动画时长、停留时间和自动文本长度计算</li><li><strong>顺序播放</strong>：字幕组按数字键顺序播放，最后一帧结束后自动关闭 HUD</li><li><strong>组级 UI 覆盖</strong>：每个字幕组可指定独立的 UI ID，不同场景使用不同样式</li><li><strong>PAPI 支持</strong>：字幕文本支持 PlaceholderAPI 变量，按目标玩家解析</li><li><strong>EventPacket 联动</strong>：其他模块（如 LoginView、OnlineRewards）可通过 EventPacket 的 <code>subtitle.play</code> 动作触发字幕组</li></ul><h2 id="依赖" tabindex="-1">依赖 <a class="header-anchor" href="#依赖" aria-label="Permalink to &quot;依赖&quot;">​</a></h2><table tabindex="0"><thead><tr><th>类型</th><th>依赖</th><th>作用</th><th>缺少时表现</th></tr></thead><tbody><tr><td>必需</td><td>ArcartX</td><td>注册 HUD 公告和字幕 UI，向客户端发送公告/字幕包</td><td>模块无法正常展示 UI</td></tr><tr><td>可选</td><td>PlaceholderAPI</td><td>解析公告、字幕文本中的 <code>%...%</code> 变量</td><td>文本照常发送，但 PAPI 变量保持原样</td></tr><tr><td>可选</td><td>EventPacket 模块</td><td>通过 <code>subtitle.play</code> 动作触发字幕组</td><td>不影响 Announcer 自身轮播</td></tr><tr><td>可选</td><td>BungeeCord / Velocity 代理</td><td>跨服广播消息转发</td><td>跨服功能不可用，本服广播正常</td></tr></tbody></table><h2 id="启用步骤" tabindex="-1">启用步骤 <a class="header-anchor" href="#启用步骤" aria-label="Permalink to &quot;启用步骤&quot;">​</a></h2><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">modules</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  announcer</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    enabled</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span></span></code></pre></div><h2 id="配置" tabindex="-1">配置 <a class="header-anchor" href="#配置" aria-label="Permalink to &quot;配置&quot;">​</a></h2><h3 id="公告主配置-arcartxannouncer-yml" tabindex="-1">公告主配置（<code>ArcartXAnnouncer.yml</code>） <a class="header-anchor" href="#公告主配置-arcartxannouncer-yml" aria-label="Permalink to &quot;公告主配置（\`ArcartXAnnouncer.yml\`）&quot;">​</a></h3><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">settings</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  debug</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">false</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                         # true 时打印发包与点击回包日志</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  ui-id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;AXS:announcer_hud&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">          # 公告 HUD 的 UI ID（支持列表格式，详见 [多 UI 同时发包](/guide/multi-ui)）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  register-ui-on-enable</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">          # 启动/重载时自动注册 HUD</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  overwrite-ui-file</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">false</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">             # 是否强制覆盖已导出的 UI 文件</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  auto-play</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                      # false 时 HUD 仍同步配置但不滚动</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  check-interval-ticks</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">20</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">             # 后台检查周期（20 tick = 1 秒）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  cooldown-ms</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">30000</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                   # 一整轮播完后的冷却时间（毫秒）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  between-entry-interval-ms</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">30000</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">     # 同一轮中两条公告之间的间隔（毫秒）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  text-width-font-size</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">60</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">             # 滚动文字宽度估算字体大小（自适应坐标），调大背景停留更久</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">entries-directory</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;announcer&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">         # 公告条目目录，相对模块数据目录</span></span></code></pre></div><p><strong>配置字段详解：</strong></p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>默认值</th><th>说明</th></tr></thead><tbody><tr><td><code>debug</code></td><td>boolean</td><td><code>false</code></td><td>开启后在控制台打印每次 <code>display</code> 发包内容和玩家点击回包信息</td></tr><tr><td><code>ui-id</code></td><td>string / list</td><td><code>&quot;AXS:announcer_hud&quot;</code></td><td>公告 HUD 的 UI ID，支持列表格式同时向多个 UI 发包</td></tr><tr><td><code>register-ui-on-enable</code></td><td>boolean</td><td><code>true</code></td><td>模块启动/重载时是否自动向 ArcartX 注册 HUD</td></tr><tr><td><code>overwrite-ui-file</code></td><td>boolean</td><td><code>false</code></td><td>是否在每次启动时强制覆盖 <code>plugins/ArcartXSuite/ui/announcer_hud.yml</code></td></tr><tr><td><code>auto-play</code></td><td>boolean</td><td><code>true</code></td><td>是否自动轮播公告；<code>false</code> 时 HUD 仍注册但不主动滚动</td></tr><tr><td><code>check-interval-ticks</code></td><td>long</td><td><code>20</code></td><td>后台定时任务检查周期（tick），20 tick = 1 秒</td></tr><tr><td><code>cooldown-ms</code></td><td>long</td><td><code>30000</code></td><td>全部公告播完一整轮后的冷却等待时间（毫秒）</td></tr><tr><td><code>between-entry-interval-ms</code></td><td>long</td><td><code>30000</code></td><td>同一轮中，上一条公告显示结束后切到下一条公告的等待时间（毫秒）</td></tr><tr><td><code>text-width-font-size</code></td><td>int</td><td><code>60</code></td><td>服务端估算滚动文字宽度的字体大小参数（自适应坐标单位）。CJK 字符宽度 ≈ 此值，Latin 字符宽度 ≈ 此值 × 0.55。调大此值会让背景条显示更久，调小则更快隐藏</td></tr><tr><td><code>entries-directory</code></td><td>string</td><td><code>&quot;announcer&quot;</code></td><td>公告条目所在目录，路径相对模块数据目录 <code>data/announcer/</code>。1.0.x 旧默认值为 <code>&quot;entries&quot;</code>，升级后旧目录会自动迁移</td></tr></tbody></table><div class="tip custom-block"><p class="custom-block-title">从 1.0.x 升级</p><p>如果你之前使用默认的 <code>entries/</code> 目录，升级到 1.1.0 后<strong>无需手动操作</strong>。模块启动时会自动检测到旧目录 <code>data/announcer/entries/</code> 并将其重命名为 <code>data/announcer/announcer/</code>，日志会输出 <code>已将旧公告目录 entries/ 迁移至 announcer/</code>。如果你在配置中自定义了 <code>entries-directory</code> 为其他名称，则不受影响。</p></div><h3 id="公告条目" tabindex="-1">公告条目 <a class="header-anchor" href="#公告条目" aria-label="Permalink to &quot;公告条目&quot;">​</a></h3><p>条目文件位于 <code>data/announcer/announcer/*.yml</code>（即 <code>entries-directory</code> 配置指向的目录）。<strong>目录下所有 <code>.yml</code> 文件都会被加载</strong>，按文件名字典序排列，同一文件可包含多条公告，根键即为条目 ID。</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># data/announcer/announcer/default.yml</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">welcome</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  enabled</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  text</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;欢迎来到服务器，祝你游玩愉快。&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # 支持颜色代码和 PAPI 变量</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  click-command</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                          # 留空表示不执行命令</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">player_tip</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  enabled</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  text</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;你好，%player_name%。记得每日签到！&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  click-command</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;say &lt;player&gt; 点击了公告&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  # &lt;player&gt; 替换为点击玩家名</span></span></code></pre></div><p>多文件示例——可将不同类型的公告分到不同文件：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>data/announcer/announcer/</span></span>
<span class="line"><span>├── default.yml         # 通用公告</span></span>
<span class="line"><span>├── events.yml          # 活动公告</span></span>
<span class="line"><span>└── tips.yml            # 小贴士</span></span></code></pre></div><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>默认值</th><th>说明</th></tr></thead><tbody><tr><td><code>enabled</code></td><td>boolean</td><td><code>true</code></td><td><code>false</code> 时跳过该条目，不下发到客户端</td></tr><tr><td><code>text</code></td><td>string</td><td><code>&quot;&quot;</code></td><td>公告文本，支持 <code>&amp;</code> 颜色代码和 PlaceholderAPI 变量</td></tr><tr><td><code>click-command</code></td><td>string</td><td><code>&quot;&quot;</code></td><td>玩家点击 HUD 时以<strong>控制台身份</strong>执行的命令，<code>&lt;player&gt;</code> 替换为点击者名字。留空表示该条公告不可点击</td></tr></tbody></table><h3 id="跨服广播配置-arcartxannouncer-yml-的-transport-proxy-节" tabindex="-1">跨服广播配置（<code>ArcartXAnnouncer.yml</code> 的 <code>transport.proxy</code> 节） <a class="header-anchor" href="#跨服广播配置-arcartxannouncer-yml-的-transport-proxy-节" aria-label="Permalink to &quot;跨服广播配置（\`ArcartXAnnouncer.yml\` 的 \`transport.proxy\` 节）&quot;">​</a></h3><p>通过 BungeeCord / Velocity 代理的 Plugin Messaging Channel 将手动广播（<code>broadcast</code> / <code>broadcastnow</code>）转发到其他子服。启用后，其他子服发出的公告也会在本服展示给在线玩家。</p><div class="warning custom-block"><p class="custom-block-title">注意</p><p><strong>仅手动广播会跨服转发</strong>，<code>entries-directory</code> 中的自动轮播条目<strong>不会</strong>跨服，各子服独立轮播。</p></div><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">transport</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  proxy</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    enabled</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">false</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                     # 是否启用 BungeeCord 代理转发</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    messenger-channel</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;AXS_ANNOUNCER&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> # BungeeCord Forward 子频道名称，所有子服必须一致</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    forward-target</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;ALL&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">              # 转发目标：ALL = 所有子服，或指定服务器名</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    node-id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                        # 当前节点 ID（用于去重），留空则自动取 server.properties 中 server-name</span></span></code></pre></div><p><strong>配置字段详解：</strong></p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>默认值</th><th>说明</th></tr></thead><tbody><tr><td><code>transport.proxy.enabled</code></td><td>boolean</td><td><code>false</code></td><td>是否启用 BungeeCord 代理转发。需要服务端运行在 BungeeCord / Velocity 代理后端</td></tr><tr><td><code>transport.proxy.messenger-channel</code></td><td>string</td><td><code>&quot;AXS_ANNOUNCER&quot;</code></td><td>BungeeCord <code>Forward</code> 子频道名称。<strong>所有子服的此值必须相同</strong>，否则消息无法互通</td></tr><tr><td><code>transport.proxy.forward-target</code></td><td>string</td><td><code>&quot;ALL&quot;</code></td><td>BungeeCord <code>Forward</code> 目标。<code>&quot;ALL&quot;</code> 表示转发给所有子服；也可填写指定服务器名（如 <code>&quot;lobby&quot;</code>）仅转发给该服</td></tr><tr><td><code>transport.proxy.node-id</code></td><td>string</td><td><code>&quot;&quot;</code></td><td>当前服务器节点标识，用于消息去重（不会处理来自自身的消息）。留空时自动使用 <code>server.properties</code> 中的 <code>server-name</code></td></tr></tbody></table><p><strong>跨服部署示例（3 台子服）：</strong></p><p>所有子服的 <code>ArcartXAnnouncer.yml</code> 中：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">transport</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  proxy</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    enabled</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    messenger-channel</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;AXS_ANNOUNCER&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    forward-target</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;ALL&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    node-id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # 留空自动取 server-name，每台服务器的 server.properties 中 server-name 需不同</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">前置条件</p><ul><li>服务端必须运行在 BungeeCord / Velocity 代理之后</li><li>BungeeCord 的 <code>config.yml</code> 中 <code>ip_forward: true</code></li><li>spigot.yml 中 <code>bungeecord: true</code></li><li>发送跨服消息时<strong>需要至少一名在线玩家</strong>充当消息载体（BungeeCord Plugin Messaging 机制限制）</li></ul></div><h3 id="跨服消息载荷格式" tabindex="-1">跨服消息载荷格式 <a class="header-anchor" href="#跨服消息载荷格式" aria-label="Permalink to &quot;跨服消息载荷格式&quot;">​</a></h3><p>跨服传输内部使用 YAML 编码的 <code>AnnouncerEnvelope</code> 消息信封：</p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>message-id</code></td><td>string</td><td>消息唯一 ID（UUID），用于接收端去重</td></tr><tr><td><code>origin-node</code></td><td>string</td><td>发送端节点 ID，接收端跳过来自自身节点的消息</td></tr><tr><td><code>text</code></td><td>string</td><td>公告文本（已渲染，不含 PAPI 变量，因为跨服后玩家上下文不同）</td></tr><tr><td><code>immediate</code></td><td>boolean</td><td><code>true</code> 表示立即广播（<code>broadcastnow</code>），<code>false</code> 表示加入队列（<code>broadcast</code>）</td></tr></tbody></table><p>去重键为 <code>originNode + &quot;:&quot; + messageId</code>，服务端维护最近 128 条消息的去重缓存（基于 LRU 策略自动淘汰）。</p><h3 id="字幕主配置-arcartxannouncer-yml-的-subtitle-节" tabindex="-1">字幕主配置（<code>ArcartXAnnouncer.yml</code> 的 <code>subtitle</code> 节） <a class="header-anchor" href="#字幕主配置-arcartxannouncer-yml-的-subtitle-节" aria-label="Permalink to &quot;字幕主配置（\`ArcartXAnnouncer.yml\` 的 \`subtitle\` 节）&quot;">​</a></h3><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">subtitle</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  settings</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    debug</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">false</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    ui-id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;AXS:subtitle_hud&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">            # 支持列表格式，详见 [多 UI 同时发包](/guide/multi-ui)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    register-ui-on-enable</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    overwrite-ui-file</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">false</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    groups-directory</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;subtitle/groups&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # 字幕组目录，相对模块数据目录</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    show-background</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                  # 是否显示字幕底部背景板</span></span></code></pre></div><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>默认值</th><th>说明</th></tr></thead><tbody><tr><td><code>debug</code></td><td>boolean</td><td><code>false</code></td><td>开启后打印每帧 <code>play</code> 和 <code>close</code> 包内容</td></tr><tr><td><code>ui-id</code></td><td>string / list</td><td><code>&quot;AXS:subtitle_hud&quot;</code></td><td>字幕 HUD 的 UI ID，支持列表格式</td></tr><tr><td><code>register-ui-on-enable</code></td><td>boolean</td><td><code>true</code></td><td>模块启动时是否自动注册字幕 HUD</td></tr><tr><td><code>overwrite-ui-file</code></td><td>boolean</td><td><code>false</code></td><td>是否强制覆盖 <code>ui/subtitle_hud.yml</code></td></tr><tr><td><code>groups-directory</code></td><td>string</td><td><code>&quot;subtitle/groups&quot;</code></td><td>字幕组文件所在目录，相对模块数据目录</td></tr><tr><td><code>show-background</code></td><td>boolean</td><td><code>true</code></td><td>是否在字幕 HUD 底部显示半透明背景板</td></tr></tbody></table><h3 id="字幕组" tabindex="-1">字幕组 <a class="header-anchor" href="#字幕组" aria-label="Permalink to &quot;字幕组&quot;">​</a></h3><p>字幕组文件位于 <code>data/announcer/subtitle/groups/*.yml</code>，<strong>文件名（不含 <code>.yml</code>）即组 ID</strong>。顶层键为数字，按数字从小到大顺序播放：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># data/announcer/subtitle/groups/welcome.yml</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 可选：组级 UI ID 覆盖，不填则使用全局 subtitle.settings.ui-id</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># ui-id: &quot;AXS:custom_subtitle_hud&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 或列表格式：</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># ui-id:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#   - &quot;AXS:subtitle_hud_1&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">#   - &quot;AXS:subtitle_hud_2&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  text</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;&amp;f欢迎，%player_name%。&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    # 支持颜色代码和 PAPI</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  length</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                          # 打字机总字数；0 = 自动按可见文本长度计算</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  time</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1400</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                         # 打字机动画时长（毫秒）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  keep</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                            # 动画结束后停留时间（秒）</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">2</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  text</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;&amp;e这是打字机字幕示例。&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  length</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">0</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  time</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1800</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  keep</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">1.5</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">                          # 最后一帧结束后自动关闭 HUD</span></span></code></pre></div><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>默认值</th><th>说明</th></tr></thead><tbody><tr><td>顶层键</td><td>int</td><td>—</td><td>数字，系统按从小到大顺序播放</td></tr><tr><td><code>text</code></td><td>string</td><td><code>&quot;&quot;</code></td><td>字幕文本，支持颜色代码和 PAPI</td></tr><tr><td><code>length</code></td><td>int</td><td><code>0</code></td><td>打字机总字数；<code>0</code> 或负数则自动按可见文本长度计算（去除颜色代码后）</td></tr><tr><td><code>time</code></td><td>int</td><td><code>1000</code></td><td>打字机动画时长（毫秒），即从 0 字到 <code>length</code> 字的过渡时间</td></tr><tr><td><code>keep</code></td><td>double</td><td><code>1.0</code></td><td>当前帧动画结束后继续停留的秒数。最后一帧的 <code>keep</code> 结束后自动关闭 HUD</td></tr><tr><td><code>ui-id</code></td><td>string / list</td><td>继承全局</td><td><strong>组级可选</strong>，指定本字幕组使用哪个 UI 播放，不填则使用 <code>subtitle.settings.ui-id</code></td></tr></tbody></table><h2 id="命令" tabindex="-1">命令 <a class="header-anchor" href="#命令" aria-label="Permalink to &quot;命令&quot;">​</a></h2><h3 id="管理命令-权限-arcartxsuite-admin" tabindex="-1">管理命令（权限：<code>arcartxsuite.admin</code>） <a class="header-anchor" href="#管理命令-权限-arcartxsuite-admin" aria-label="Permalink to &quot;管理命令（权限：\`arcartxsuite.admin\`）&quot;">​</a></h3><table tabindex="0"><thead><tr><th>命令</th><th>说明</th></tr></thead><tbody><tr><td><code>/axs announcer help</code></td><td>查看 Announcer 模块所有可用命令</td></tr><tr><td><code>/axs announcer status</code></td><td>查看模块状态：活跃公告数、已初始化玩家数、字幕组数、播放中玩家数、待播队列和跨服传输状态</td></tr><tr><td><code>/axs reload announcer</code></td><td>重载配置和 HUD（公告 + 字幕一起重载），HUD 不中断</td></tr><tr><td><code>/axs announcer broadcast &lt;文本&gt;</code></td><td><strong>本服</strong>将一条自定义文本加入广播队列，当前公告展示结束后立即播报</td></tr><tr><td><code>/axs announcer broadcastnow &lt;文本&gt;</code></td><td><strong>本服</strong>立即广播，强制打断当前正在展示的公告</td></tr><tr><td><code>/axs announcer gbroadcast &lt;文本&gt;</code></td><td><strong>跨服</strong>加入广播队列，同时通过代理转发到其他子服（需启用 <code>transport.proxy</code>）</td></tr><tr><td><code>/axs announcer gbroadcastnow &lt;文本&gt;</code></td><td><strong>跨服</strong>立即广播，强制打断当前展示并转发到其他子服（远端也立即广播）</td></tr><tr><td><code>/axs announcer subtitle list</code></td><td>列出所有已加载的字幕组 ID</td></tr><tr><td><code>/axs announcer subtitle play &lt;玩家&gt; &lt;字幕组ID&gt;</code></td><td>向在线玩家播放指定字幕组。如果该玩家有字幕正在播放，会先终止旧的</td></tr><tr><td><code>/axs announcer subtitle stop &lt;玩家&gt;</code></td><td>立即停止玩家当前正在播放的字幕并关闭字幕 HUD</td></tr></tbody></table><p><strong><code>status</code> 输出示例：</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>◆ ArcartXSuite | Announcer 状态</span></span>
<span class="line"><span>◆ ArcartXSuite | 活跃公告: 2</span></span>
<span class="line"><span>◆ ArcartXSuite | 已初始化玩家: 5</span></span>
<span class="line"><span>◆ ArcartXSuite | 字幕组: 3</span></span>
<span class="line"><span>◆ ArcartXSuite | 字幕播放中: 1</span></span>
<span class="line"><span>◆ ArcartXSuite | 待播队列: 0</span></span>
<span class="line"><span>◆ ArcartXSuite | 跨服传输: 已启用</span></span></code></pre></div><p><strong>命令示例：</strong></p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 查看状态</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">/axs</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> announcer</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> status</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 本服维护通知（加入队列，仅本服播报）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">/axs</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> announcer</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> broadcast</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">c服务器将于10分钟后维护，请及时保存进度！</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 本服紧急通知（立即打断当前公告，仅本服）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">/axs</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> announcer</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> broadcastnow</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">c</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&amp;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">l紧急通知：服务器即将重启</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 跨服广播（加入队列，同时转发到所有子服）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">/axs</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> announcer</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> gbroadcast</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">c全服维护通知，请及时保存进度！</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 跨服立即广播（所有子服立即打断当前展示）</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">/axs</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> announcer</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> gbroadcastnow</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">c</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">&amp;</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">l紧急通知：全服即将重启</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 播放字幕</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">/axs</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> announcer</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> subtitle</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> play</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Steve</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> welcome</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 停止字幕</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">/axs</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> announcer</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> subtitle</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> stop</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> Steve</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># 重载配置</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">/axs</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> reload</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> announcer</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">重载命令</p><p>Announcer 模块的重载命令是 <code>/axs reload announcer</code>（全局统一格式），而非 <code>/axs announcer reload</code>。在模块子命令中输入 <code>reload</code> 会提示正确用法。</p></div><h2 id="ui-packet" tabindex="-1">UI / Packet <a class="header-anchor" href="#ui-packet" aria-label="Permalink to &quot;UI / Packet&quot;">​</a></h2><table tabindex="0"><thead><tr><th>功能</th><th>UI ID</th><th>默认打开方式</th><th>Packet 说明</th></tr></thead><tbody><tr><td>公告 HUD</td><td><code>AXS:announcer_hud</code></td><td><code>defaultOpen: true</code>（自动打开）</td><td>服务端推 <code>display</code> 包控制滚动文字显示；客户端点击时推 <code>AXS_announcer_click</code> 回包</td></tr><tr><td>字幕 HUD</td><td><code>AXS:subtitle_hud</code></td><td><code>defaultOpen: false</code>（按需打开）</td><td>服务端按帧推 <code>play</code> 包驱动打字机动画；字幕组播完推 <code>close</code> 包关闭 HUD</td></tr></tbody></table><h3 id="公告-hud-display-包字段" tabindex="-1">公告 HUD <code>display</code> 包字段 <a class="header-anchor" href="#公告-hud-display-包字段" aria-label="Permalink to &quot;公告 HUD \`display\` 包字段&quot;">​</a></h3><p>服务端每次切换公告或手动广播时向客户端推送：</p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>isShow</code></td><td>boolean</td><td><code>true</code> 显示公告并开始滚动，<code>false</code> 隐藏 HUD</td></tr><tr><td><code>id</code></td><td>string</td><td>当前公告条目 ID（手动广播时为 <code>&quot;manual&quot;</code>）</td></tr><tr><td><code>text</code></td><td>string</td><td>公告文本（已解析 PAPI 和颜色代码）</td></tr><tr><td><code>clickable</code></td><td>boolean</td><td>该条公告是否可点击（即 <code>click-command</code> 非空）。手动广播和跨服接收的公告 <code>clickable</code> 始终为 <code>false</code></td></tr><tr><td><code>revision</code></td><td>string</td><td>服务端时间戳 + 序号，用于客户端去重</td></tr><tr><td><code>textWidth</code></td><td>int</td><td>服务端估算的文字渲染宽度（自适应坐标），客户端用于判断滚动结束和隐藏背景</td></tr></tbody></table><p>客户端点击时向服务端发送 <code>AXS_announcer_click</code> 包，携带当前条目 ID。</p><h3 id="字幕-hud-play-包字段" tabindex="-1">字幕 HUD <code>play</code> 包字段 <a class="header-anchor" href="#字幕-hud-play-包字段" aria-label="Permalink to &quot;字幕 HUD \`play\` 包字段&quot;">​</a></h3><p>服务端每播放一帧字幕时推送：</p><table tabindex="0"><thead><tr><th>字段</th><th>类型</th><th>说明</th></tr></thead><tbody><tr><td><code>text</code></td><td>string</td><td>当前帧字幕文本（已解析 PAPI）</td></tr><tr><td><code>length</code></td><td>int</td><td>打字机总字数</td></tr><tr><td><code>time</code></td><td>int</td><td>动画时长（毫秒）</td></tr><tr><td><code>showBackground</code></td><td>boolean</td><td>是否显示背景板（来自全局配置 <code>subtitle.settings.show-background</code>）</td></tr></tbody></table><p>字幕组全部帧播完后，服务端推送空的 <code>close</code> 包，客户端重置字幕状态。</p><h2 id="eventpacket-联动" tabindex="-1">EventPacket 联动 <a class="header-anchor" href="#eventpacket-联动" aria-label="Permalink to &quot;EventPacket 联动&quot;">​</a></h2><p>Announcer 通过 <code>SubtitlePlayable</code> capability 向 EventPacket 注册，支持以下动作：</p><table tabindex="0"><thead><tr><th>动作类型</th><th>参数</th><th>说明</th></tr></thead><tbody><tr><td><code>subtitle.play</code></td><td><code>group-id</code></td><td>向触发玩家播放指定字幕组</td></tr></tbody></table><p>EventPacket 配置示例（<code>data/eventpacket/rules/join.yml</code>）：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">join_welcome_subtitle</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  enabled</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  trigger</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">join</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  repeatable</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  actions</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">type</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">subtitle.play</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">      group-id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;welcome&quot;</span></span></code></pre></div><h2 id="完整运行流程" tabindex="-1">完整运行流程 <a class="header-anchor" href="#完整运行流程" aria-label="Permalink to &quot;完整运行流程&quot;">​</a></h2><h3 id="公告轮播流程" tabindex="-1">公告轮播流程 <a class="header-anchor" href="#公告轮播流程" aria-label="Permalink to &quot;公告轮播流程&quot;">​</a></h3><ol><li>模块启动 → 从 <code>entries-directory</code> 加载所有 <code>.yml</code> 文件中 <code>enabled: true</code> 的条目</li><li>服务端定时任务每 <code>check-interval-ticks</code> tick 运行一次</li><li>到达播出时间 → 取下一条公告 → 向所有在线玩家推送 <code>display</code> 包</li><li>客户端收到 <code>display</code> 包 → 文字从屏幕右侧开始向左滚动 → 滚出左边界后自动隐藏背景</li><li>等待 <code>between-entry-interval-ms</code> → 播下一条</li><li>全部条目播完 → 等待 <code>cooldown-ms</code> → 从头开始</li></ol><div class="info custom-block"><p class="custom-block-title">INFO</p><p>自动轮播<strong>不会</strong>跨服转发，各子服独立轮播各自的公告条目。</p></div><h3 id="手动广播流程" tabindex="-1">手动广播流程 <a class="header-anchor" href="#手动广播流程" aria-label="Permalink to &quot;手动广播流程&quot;">​</a></h3><ol><li>管理员执行 <code>/axs announcer broadcast 维护通知</code></li><li>文本加入本地内部队列</li><li>下一次定时任务检查时优先消费队列 → 推送 <code>display</code> 包（<code>id</code> = <code>&quot;manual&quot;</code>）</li></ol><h3 id="跨服广播流程" tabindex="-1">跨服广播流程 <a class="header-anchor" href="#跨服广播流程" aria-label="Permalink to &quot;跨服广播流程&quot;">​</a></h3><ol><li>管理员执行 <code>/axs announcer gbroadcast 维护通知</code></li><li>文本加入本地内部队列</li><li>封装为 <code>AnnouncerEnvelope</code>（<code>immediate=false</code>）→ 通过 BungeeCord Forward 发送到其他子服</li><li>本服下一次定时任务检查时优先消费队列 → 推送 <code>display</code> 包（<code>id</code> = <code>&quot;manual&quot;</code>）</li><li>其他子服收到信封 → 去重校验 → 加入当地广播队列 → 按同样流程展示</li></ol><h3 id="立即广播流程" tabindex="-1">立即广播流程 <a class="header-anchor" href="#立即广播流程" aria-label="Permalink to &quot;立即广播流程&quot;">​</a></h3><ol><li>管理员执行 <code>/axs announcer broadcastnow 紧急通知</code></li><li>立即打断当前展示，向所有在线玩家推送 <code>display</code> 包（仅本服）</li></ol><h3 id="跨服立即广播流程" tabindex="-1">跨服立即广播流程 <a class="header-anchor" href="#跨服立即广播流程" aria-label="Permalink to &quot;跨服立即广播流程&quot;">​</a></h3><ol><li>管理员执行 <code>/axs announcer gbroadcastnow 紧急通知</code></li><li>本服立即打断当前展示，向所有在线玩家推送 <code>display</code> 包</li><li>封装为 <code>AnnouncerEnvelope</code>（<code>immediate=true</code>）→ 通过 BungeeCord Forward 发送到其他子服</li><li>其他子服收到信封 → 去重校验 → <strong>立即</strong>在当地广播（也打断当前展示）</li></ol><h3 id="跨服消息收发流程" tabindex="-1">跨服消息收发流程 <a class="header-anchor" href="#跨服消息收发流程" aria-label="Permalink to &quot;跨服消息收发流程&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>子服 A                          BungeeCord 代理                      子服 B</span></span>
<span class="line"><span>  │                                  │                                 │</span></span>
<span class="line"><span>  │  gbroadcast &quot;维护通知&quot;           │                                 │</span></span>
<span class="line"><span>  │─────────────────────────────────&gt;│                                 │</span></span>
<span class="line"><span>  │  Forward / AXS_ANNOUNCER         │    Plugin Message               │</span></span>
<span class="line"><span>  │  AnnouncerEnvelope(YAML)         │────────────────────────────────&gt;│</span></span>
<span class="line"><span>  │                                  │                                 │</span></span>
<span class="line"><span>  │                                  │    去重 → originNode ≠ 自身     │</span></span>
<span class="line"><span>  │                                  │    immediate=false → 入队       │</span></span>
<span class="line"><span>  │                                  │    → 下次 tick 展示给在线玩家    │</span></span></code></pre></div><p><strong>去重机制</strong>：</p><ul><li>每个信封携带 UUID <code>messageId</code> 和 <code>originNode</code></li><li>接收端维护最近 128 条的 <code>originNode:messageId</code> LRU 去重集合</li><li>已处理过的信封静默丢弃，防止因网络重发导致重复广播</li><li>来自自身节点的信封（<code>originNode</code> 与本服 <code>nodeId</code> 相同）直接跳过</li></ul><h3 id="字幕播放流程" tabindex="-1">字幕播放流程 <a class="header-anchor" href="#字幕播放流程" aria-label="Permalink to &quot;字幕播放流程&quot;">​</a></h3><ol><li>管理员或 EventPacket 触发 <code>/axs announcer subtitle play Steve welcome</code></li><li>服务端查找 <code>welcome.yml</code> 字幕组</li><li>按数字键顺序依次推送 <code>play</code> 包</li><li>客户端每帧执行打字机动画（<code>Lerp</code> 补间），动画结束后停留 <code>keep</code> 秒</li><li>全部帧播完 → 服务端推送 <code>close</code> 包 → 客户端重置字幕 HUD</li></ol><h2 id="技术架构" tabindex="-1">技术架构 <a class="header-anchor" href="#技术架构" aria-label="Permalink to &quot;技术架构&quot;">​</a></h2><h3 id="类结构" tabindex="-1">类结构 <a class="header-anchor" href="#类结构" aria-label="Permalink to &quot;类结构&quot;">​</a></h3><table tabindex="0"><thead><tr><th>类</th><th>所在包</th><th>职责</th></tr></thead><tbody><tr><td><code>AnnouncerModule</code></td><td><code>announcer</code></td><td>模块入口，继承 <code>AbstractAXSModule</code>；管理配置加载、UI 注册、服务生命周期、跨服传输启停和命令处理</td></tr><tr><td><code>AnnouncerModuleConfiguration</code></td><td><code>announcer.config</code></td><td>配置 record，解析 <code>ArcartXAnnouncer.yml</code> 中的公告设置、跨服配置和字幕设置</td></tr><tr><td><code>AnnouncerProxyConfiguration</code></td><td><code>announcer.config</code></td><td>跨服代理配置 record（<code>enabled</code>、<code>messengerChannel</code>、<code>forwardTarget</code>、<code>nodeId</code>）</td></tr><tr><td><code>AnnouncerEntry</code></td><td><code>announcer.config</code></td><td>单条公告条目 record（<code>id</code>、<code>enabled</code>、<code>text</code>、<code>clickCommand</code>）</td></tr><tr><td><code>AnnouncerService</code></td><td><code>announcer.service</code></td><td>公告核心服务：轮播调度、手动广播、跨服转发/接收、客户端点击处理</td></tr><tr><td><code>SubtitleService</code></td><td><code>announcer.service</code></td><td>字幕服务：组加载、逐帧播放、HUD 开关</td></tr><tr><td><code>AnnouncerProxyTransport</code></td><td><code>announcer.transport</code></td><td>BungeeCord Plugin Messaging Channel 实现：注册频道、发送 <code>Forward</code> 消息、接收并解码回调</td></tr><tr><td><code>AnnouncerEnvelope</code></td><td><code>announcer.transport</code></td><td>跨服消息载荷 record（<code>messageId</code>、<code>originNode</code>、<code>text</code>、<code>immediate</code>）</td></tr><tr><td><code>AnnouncerEnvelopeCodec</code></td><td><code>announcer.transport</code></td><td>信封的 YAML 序列化/反序列化工具类</td></tr></tbody></table></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("modules/announcer.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const announcer = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  announcer as default
};
