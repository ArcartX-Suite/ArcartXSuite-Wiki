import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"多 UI 同时发包","description":"","frontmatter":{},"headers":[],"relativePath":"guide/multi-ui.md","filePath":"guide/multi-ui.md","lastUpdated":1779445924000}');
const _sfc_main = { name: "guide/multi-ui.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="多-ui-同时发包" tabindex="-1">多 UI 同时发包 <a class="header-anchor" href="#多-ui-同时发包" aria-label="Permalink to &quot;多 UI 同时发包&quot;">​</a></h1><p>ArcartXSuite 大多数模块的 <code>ui-id</code> 字段都支持<strong>字符串</strong>和<strong>列表</strong>两种格式。当配置为列表时，服务端会把同一个 payload 同时发送给列表中的每一个 UI，适用于一份数据同时驱动多套 UI 展现的场景。</p><h2 id="适用场景" tabindex="-1">适用场景 <a class="header-anchor" href="#适用场景" aria-label="Permalink to &quot;适用场景&quot;">​</a></h2><ul><li>同一个功能需要在<strong>全屏菜单</strong>和<strong>小地图 HUD</strong> 上同时展示数据</li><li>你为某个模块制作了多套风格的 UI 文件，希望一次性推送数据到所有 UI</li><li>Tab 定义需要同时推送到在线列表和竞技场积分板</li></ul><h2 id="配置格式" tabindex="-1">配置格式 <a class="header-anchor" href="#配置格式" aria-label="Permalink to &quot;配置格式&quot;">​</a></h2><p>所有支持多 UI 的字段都使用统一的解析器（<code>UiIdParser</code>），兼容两种写法：</p><h3 id="单-ui-默认写法" tabindex="-1">单 UI（默认写法） <a class="header-anchor" href="#单-ui-默认写法" aria-label="Permalink to &quot;单 UI（默认写法）&quot;">​</a></h3><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">ui-id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;AXS:boss_tracker&quot;</span></span></code></pre></div><h3 id="多-ui-列表写法" tabindex="-1">多 UI（列表写法） <a class="header-anchor" href="#多-ui-列表写法" aria-label="Permalink to &quot;多 UI（列表写法）&quot;">​</a></h3><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">ui-id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;AXS:boss_tracker&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  - </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;AXS:boss_tracker_alt&quot;</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">向后兼容</p><p>从单 UI 切换到多 UI <strong>不需要修改代码或 migration</strong>，只需把配置值改为列表即可。服务端内部统一处理为 <code>List&lt;String&gt;</code>。</p></div><h2 id="支持的模块与字段" tabindex="-1">支持的模块与字段 <a class="header-anchor" href="#支持的模块与字段" aria-label="Permalink to &quot;支持的模块与字段&quot;">​</a></h2><table tabindex="0"><thead><tr><th>模块</th><th>配置文件</th><th>支持多 UI 的字段</th></tr></thead><tbody><tr><td><strong>Tab</strong></td><td><code>tabs/*.yml</code></td><td><code>ui-targets[].ui-id</code>（或旧格式 <code>ui-id</code>）</td></tr><tr><td><strong>EntityTracker</strong></td><td><code>ArcartXEntityTracker.yml</code></td><td><code>boss.settings.ui-id</code>、<code>attack-target.settings.ui-id</code></td></tr><tr><td><strong>Announcer</strong></td><td><code>ArcartXAnnouncer.yml</code></td><td><code>settings.ui-id</code>、<code>subtitle.settings.ui-id</code></td></tr><tr><td><strong>Conversation</strong></td><td><code>ArcartXConversation.yml</code></td><td><code>client.dialog-ui-id</code>、<code>client.selector-ui-id</code></td></tr><tr><td><strong>QuestGPS</strong></td><td><code>ArcartXQuestGPS.yml</code></td><td><code>client.menu-ui-id</code>、<code>client.guide-ui-id</code></td></tr><tr><td><strong>OnlineRewards</strong></td><td><code>ArcartXOnlineRewards.yml</code></td><td><code>ui.menu-ui-id</code></td></tr><tr><td><strong>Subtitle 字幕组</strong></td><td><code>subtitle/groups/*.yml</code></td><td>每个字幕组的 <code>ui-id</code>（覆盖全局默认）</td></tr></tbody></table><div class="info custom-block"><p class="custom-block-title">Tab 的特殊格式</p><p>Tab 模块除了通用的 <code>ui-id</code> 字段外，还支持更灵活的 <code>ui-targets</code> 列表格式，每个 target 可独立指定 <code>packet-handler</code>：</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">ui-targets</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">ui-id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;tab&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    packet-handler</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;tab&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">ui-id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;tab-arena&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    packet-handler</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;arena&quot;</span></span></code></pre></div><p><code>ui-targets</code> 优先级高于 <code>ui-id</code> + <code>packet-handler</code>，两者不要同时写。</p></div><h2 id="工作原理" tabindex="-1">工作原理 <a class="header-anchor" href="#工作原理" aria-label="Permalink to &quot;工作原理&quot;">​</a></h2><ol><li>模块启动时，为列表中每个 <code>ui-id</code> 分别向 ArcartX 注册 UI 文件</li><li>发包时调用 <code>ArcartXPacketBridge.sendPacketToAll(player, uiIds, handler, payload)</code> 遍历所有 UI 发送</li><li>打开 / 关闭 UI 同样通过 <code>openUiAll</code> / <code>closeUiAll</code> 批量操作</li><li>客户端回包（<code>Packet.send</code>）仍然只发给配置中的 <code>packet-id</code>，服务端统一处理</li></ol><h2 id="注意事项" tabindex="-1">注意事项 <a class="header-anchor" href="#注意事项" aria-label="Permalink to &quot;注意事项&quot;">​</a></h2><ul><li>列表中的每个 <code>ui-id</code> 必须对应一个实际存在的 UI 文件，否则注册会失败</li><li>客户端回包的 <code>packet-id</code> 不受多 UI 影响，仍然使用配置中的单一 <code>packet-id</code> 字段</li><li>多 UI 发包只是服务端广播同一份 payload，不会为不同 UI 生成不同的数据</li><li>首个 UI ID 会作为向后兼容的 &quot;主 UI ID&quot;，用于日志输出和 PAPI 等需要单值的场景</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("guide/multi-ui.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const multiUi = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  multiUi as default
};
